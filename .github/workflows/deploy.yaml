name: test API
on: 
  push:
  workflow_dispatch: # Corrected from workflow-dispatch
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Get code
        uses: actions/checkout@v3

      - name: Install dependencies
        run: npm ci

      - name: Run API tests
        run: npm run test:api
